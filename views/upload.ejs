<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="../public/assets/style.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload File</title>
</head>
<body>
    <div class="header">
        <a href="/folders"><h2>File Uploader</h2></a>
        <a href="/logout">Log Out</a>
    </div>
    <%if(!req.params.id){%>
    <div class="loginBody">
        <form action="/upload" method="post" enctype="multipart/form-data" class="login">
           <div class="userpass">
            <label for="file">Select a file to upload</label>
             <input type="file" name="file" required/>
             <button type="submit" class="loginButton">Upload</button>
           </div>
        </form>
    </div>
    <%}else{%>
        <div class="loginBody">
            <form action="/upload/<%=req.params.id%>" method="post" enctype="multipart/form-data" class="login">
                <div class="userpass">
                    <label for="file">Select a file to upload</label>
                    <input type="file" name="file" />
                    <button type="submit" class="loginButton">Submit</button>
                </div>
            </form>
        </div>
    <%}%>
</body>
</html>